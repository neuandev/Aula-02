# Aula 02

## Executar a página

Abra o projeto no VS Code e use a extensão **Live Server** para visualizar o HTML.

## Executar o Node.js

No terminal, dentro desta pasta:

```bash
npm install
npm start
```

O terminal exibirá as mensagens coloridas usando o pacote `chalk`.
